<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
    </style>


    <!--
      Capacitor's template sets android:background="@drawable/splash" here, which
      Android 12+ ignores: from then on the system draws the splash itself and
      only reads the windowSplashScreen* attributes. targetSdk is 36, so on every
      device this app supports the template line did nothing and the launch fell
      back to a bare system default.

      The splash icon is its own plate, not the launcher foreground. That canvas
      is 288dp with content inside the inner 192dp, which at 450dpi is 810px:
      the 192px launcher plate was being upscaled 4.2x, and the mark landed at
      363px of the 540px allowed. drawable-nodpi/splash_icon.png is 1152px, and
      inverts per theme: the light ground takes the dark mark.
    -->
    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/splash_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_icon</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>