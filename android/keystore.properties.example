# Copy to android/keystore.properties and fill in. That file is gitignored.
# storeFile is resolved relative to the android/ directory; an absolute path
# pointing outside the repo is safer still.
storeFile=polon-upload-key.jks
storePassword=
keyAlias=polon-upload
keyPassword=
